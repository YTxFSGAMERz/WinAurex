<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinAurex_App.Views.OptimizationProfilesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:WinAurex_App.Controls"
    xmlns:models="using:WinAurex_App.Models"
    xmlns:systemModels="using:WinAurex.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Spacing="{StaticResource SpacingLarge}">
            <!-- Header -->
            <StackPanel Spacing="{StaticResource SpacingSmall}">
                <TextBlock Text="Optimization Profiles" Style="{StaticResource TitleTextStyle}" />
                <TextBlock Text="Pre-configured profiles tailored for specific use cases. Select a profile to view its tweaks before applying." Style="{StaticResource BodyTextStyle}" Foreground="{ThemeResource TextSecondaryBrush}" TextWrapping="Wrap" />
            </StackPanel>
        </StackPanel>
        
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,24,0,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
            <ItemsControl ItemsSource="{x:Bind ViewModel.Profiles, Mode=OneWay}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="models:OptimizationProfile">
                        <Expander IsExpanded="{x:Bind IsExpanded, Mode=TwoWay}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Margin="0,0,0,16">
                            <Expander.Header>
                                <Grid HorizontalAlignment="Stretch" Margin="0,8" ColumnSpacing="16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    
                                    <Border Grid.Column="0" Background="{ThemeResource SystemControlBackgroundAccentBrush}" Width="48" Height="48" CornerRadius="8" VerticalAlignment="Center">
                                        <FontIcon Glyph="{x:Bind Icon}" FontSize="24" Foreground="White" />
                                    </Border>
                                    
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                                        <TextBlock Text="{x:Bind Name}" Style="{StaticResource SubtitleTextStyle}" />
                                        <TextBlock Text="{x:Bind Description}" Style="{StaticResource BodyTextStyle}" Foreground="{ThemeResource TextSecondaryBrush}" TextWrapping="Wrap" />
                                    </StackPanel>
                                    
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <Button Content="Apply Profile" Command="{x:Bind ApplyProfileCommand}" Style="{StaticResource AccentButtonStyle}" />
                                        <Button Content="Revert Profile" Command="{x:Bind RevertProfileCommand}" />
                                    </StackPanel>
                                </Grid>
                            </Expander.Header>
                            <ItemsControl ItemsSource="{x:Bind Tweaks}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="systemModels:SystemAction">
                                        <controls:SettingCard Action="{Binding}" Margin="0,0,0,8" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Expander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
