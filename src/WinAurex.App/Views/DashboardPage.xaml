<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinAurex_App.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:WinAurex_App.Controls"
    xmlns:models="using:WinAurex.Models"
    xmlns:viewmodels="using:WinAurex_App.ViewModels"
    mc:Ignorable="d"
    HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Spacing="{StaticResource SpacingLarge}">
            <!-- Header -->
            <StackPanel Spacing="{StaticResource SpacingSmall}">
                <TextBlock Text="Welcome to WinAurex" Style="{StaticResource TitleTextStyle}" />
                <TextBlock Text="Select and apply tweaks to optimize your Windows experience." Style="{StaticResource BodyTextStyle}" Foreground="{ThemeResource TextSecondaryBrush}" />
            </StackPanel>

            <!-- Search and Filter -->
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <TextBox PlaceholderText="Search tweaks..." 
                         Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         VerticalAlignment="Center">
                </TextBox>
            </Grid>
        </StackPanel>

        <!-- Tweaks List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,24,0,0">
            <ItemsControl ItemsSource="{x:Bind ViewModel.FilteredGroups, Mode=OneWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="viewmodels:TweakGroup">
                        <StackPanel Margin="0,0,0,24">
                            <!-- Category Header -->
                            <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,0,0,12">
                                <FontIcon Glyph="{x:Bind CategoryIcon}" FontSize="20" Foreground="{ThemeResource TextPrimaryBrush}" VerticalAlignment="Center" />
                                <TextBlock Text="{x:Bind Category}" Style="{StaticResource SubtitleTextBlockStyle}" />
                                <Border Background="{ThemeResource SystemControlBackgroundAccentBrush}" 
                                        CornerRadius="12" 
                                        Padding="8,2"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind Tweaks.Count}" 
                                             Foreground="White"
                                             FontSize="12" 
                                             FontWeight="Bold"/>
                                </Border>
                            </StackPanel>

                            <!-- Tweaks in this Category -->
                            <Border Background="{ThemeResource CardBackgroundBrush}"
                                    BorderBrush="{ThemeResource CardBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="{StaticResource CardCornerRadius}"
                                    Padding="16">
                                <ItemsControl ItemsSource="{x:Bind Tweaks}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="models:SystemAction">
                                            <controls:SettingCard Action="{Binding}" Margin="0,0,0,8" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Border>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
