<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinAurex_App.Views.AppShell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <NavigationView x:Name="RootNavigationView"
                        IsBackButtonVisible="Collapsed"
                        SelectionChanged="RootNavigationView_SelectionChanged"
                        PaneDisplayMode="Left">
            <NavigationView.MenuItems>
                <NavigationViewItem Tag="dashboard" Icon="Home" Content="Dashboard" />
                <NavigationViewItem Tag="profiles" Icon="List" Content="Optimization Profiles" />
            </NavigationView.MenuItems>
            <NavigationView.FooterMenuItems>
                <!-- WinUI automatically provides a Settings item -->
            </NavigationView.FooterMenuItems>
            
            <Grid Padding="24,24,24,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                
                <BreadcrumbBar x:Name="AppBreadcrumbBar" Margin="0,0,0,16" />
                
                <Frame x:Name="ContentFrame" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" />
            </Grid>
        </NavigationView>
    </Grid>
</Page>
